// 화면 항상 켜짐 / 해제 SDK 래퍼.
// SDK v2.x의 정확한 export는 가이드 확인 필요.
export async function setKeepScreenOn(_on: boolean): Promise<void> {
  // TODO(toss): SDK v2 screen.setKeepScreenOn 또는 RN-keep-awake로 교체.
}
